{% extends 'base.html.twig' %}

{% block title %}Mentions légales{% endblock %}

{% block body %}
    <h1>Mentions légales</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ legal_notice.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ legal_notice.title }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ legal_notice.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('legal_notice_index') }}">Retour à la liste</a>

    <a href="{{ path('legal_notice_edit', {'id': legal_notice.id}) }}">Modifier</a>

    {{ include('legal_notice/_delete_form.html.twig') }}
{% endblock %}
